<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stylish Form</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            background: #f2f2f2;
        }
        .form-container {
            background: #fff;
            padding: 20px 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            text-align: center;
        }
        .form-container h2 {
            color: #333;
        }
        .input-field {
            width: 100%;
            padding: 10px;
            margin-top: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
            outline: none;
            transition: border-color 0.3s;
        }
        .input-field:focus {
            border-color: #4a90e2;
        }
        .submit-btn {
            margin-top: 15px;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            background-color: #4a90e2;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .submit-btn:hover {
            background-color: #357ab8;
        }
        .too{
            text-align: left;
            height: 200;
            font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
            margin-top: 250px;
            justify-content: center;
        }
    </style>
</head>
<body>
 
    
    <div class="form-container">
        <h2>Enter Your Url</h2>
   
        <form action="url" method="POST">
            <input type="text" name="url" class="input-field"  placeholder="Type your url here" required>
            <button type="submit" class="submit-btn">get</button>
        </form>
        <div class="too">
            <% if (locals.id) { %>
                <p>URL GENRATED  : https://www.example.com  :-<%= id %></p>
             
            <% } %>
        </div>
       
    </div>
    <div class="">
        <table>
          <% if (locals.urls) { %>
            <thead>
                <th>S.no</th>
                <th>Shortid</th>
                <th>Redirect URl</th>
                <th>Clikcs</th>
            </thead>
            <tbody>
                <tr>
                    <% urls.forEach((url,index)=> { %>
                        <li><%= index + 1 %></li>
                        <li><%= url.shortId %></li>
                        <li><%= url.redirectUrl %></li>
                        <li><%= url.VisiteHistory.length %></li>
                     
                    <% }) %>
                </tr>
            </tbody>
           
          <% } %>
        </table>
    </div>
</body>
</html>
